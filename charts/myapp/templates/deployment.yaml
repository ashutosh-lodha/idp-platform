apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ .Release.Name }}
  namespace: idp
  labels:
    app: {{ .Release.Name }}
    env: {{ .Values.env }}
    managed-by: idp
    source: {{ .Values.source }}

spec:
  replicas: {{ .Values.replicaCount }}

  selector:
    matchLabels:
      app: {{ .Release.Name }}

  template:
    metadata:
      labels:
        app: {{ .Release.Name }}
        env: {{ .Values.env }}
        managed-by: idp
        source: {{ .Values.source }}

    spec:
      containers:
        - name: {{ .Release.Name }}

          image: {{ .Values.image.repository }}:{{ .Values.image.tag }}
          imagePullPolicy: IfNotPresent

          ports:
            - containerPort: {{ .Values.service.port }}

          # ENV VARIABLES (optional)
          {{- if .Values.envVars }}
          env:
          {{- range $key, $value := .Values.envVars }}
            - name: {{ $key }}
              value: {{ $value | quote }}
          {{- end }}
          {{- end }}

          # SECRETS (optional - FIXED)
          {{- if .Values.secrets }}
          envFrom:
            - secretRef:
                name: {{ .Release.Name }}-secret
          {{- end }}

          # HEALTH CHECKS
          readinessProbe:
            httpGet:
              path: /
              port: {{ .Values.service.port }}
            initialDelaySeconds: 2
            periodSeconds: 5

          livenessProbe:
            httpGet:
              path: /
              port: {{ .Values.service.port }}
            initialDelaySeconds: 5
            periodSeconds: 10

          # RESOURCES (fixed indentation)
          resources:
            limits:
              cpu: {{ .Values.resources.limits.cpu }}
              memory: {{ .Values.resources.limits.memory }}
            requests:
              cpu: {{ .Values.resources.requests.cpu }}
              memory: {{ .Values.resources.requests.memory }}